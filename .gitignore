# macOS
.DS_Store

# Editor / tooling (not part of the website)
.claude/

# Heavy photo originals stay local as masters — the site only needs photos/web/.
# (Removed from the repo + deploy on 2026-06-29; files remain on disk as backup.)
photos/*.jpeg
photos/*.jpg
photos/*.JPG
photos/*.png
