# Netlify configuration for Ahora
# Static site + a tiny Node build that compiles the album and the published
# blogs into published.json. Serverless functions handle the GitHub OAuth
# login for /admin.

[build]
  command = "node build-content.js"
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
